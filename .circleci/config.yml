version: 2
jobs:
  build:
    branches:
      only:
        - master
        - improve-webpack-build-system
    docker:
      - image: cibuilds/hugo:latest
    steps:
      - checkout
      - run: apk add --update npm
      - run: ./build-and-push-to-gh-pages.bash
